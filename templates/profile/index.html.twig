{% extends 'base.html.twig' %}

{% block title %}Profil{% endblock %}

{% block body %}
    <div class="container d-flex justify-content-center align-items-center" style="min-height: 40vh;">
        <div class="text-center">
            <h1 class="my-5">Bienvenue {{ user.lastname }} </h1>
            <img src="{{ asset('assets/images/iconeutilisateur.png') }}" class="mb-3" alt="Photo de profil">
            <div>Nom: {{ user.firstname }}</div>
            <div>Prénom: {{ user.lastname }}</div>
            <div>Date de naissance: {{  user.dateOfBirth|date('d-m-Y')}}</div>
            <div  class="mb-5">Email : {{ user.email }}</div>
            <a href="{# {{ path('edit_profil') }} #}" class="btn btn-primary">Modifier le profil</a>
            <a href="{# {{ path('delete_profil') }} #}" class="btn btn-danger">Supprimer le profil</a>
        </div>
    </div>
{% endblock %}
