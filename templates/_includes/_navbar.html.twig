<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="{{ path('app_home') }}">
        <img src={{ asset('assets/images/logo3.jpg') }}
             width="70" height="70" class="d-inline-block align-top mx-4" alt="">
    </a>

    <button class="navbar-toggler" type="button" data-toggle="collapse"
            data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
            aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item active">
                <a class="nav-link" href="{{ path('app_home') }}">Accueil</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_shop') }}">Nos produits</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_history') }}">Qui sommes-nous ?</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_contact') }}">Contactez-nous</a>
            </li>
        </ul>

        <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
            {% if app.user %}
                {% if app.user.isVerified %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_profile') }}">Mon compte</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}">Me déconnecter</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link disabled" href="#" tabindex="-1" aria-disabled="true">Mon compte</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}">Me déconnecter</a>
                    </li>
                {% endif %}
            {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_login') }}">Me connecter</a>
                </li>
            {% endif %}

        </ul>
    </div>
</nav>